apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-service
  name: hello-service
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: hello-service
  type: ClusterIP