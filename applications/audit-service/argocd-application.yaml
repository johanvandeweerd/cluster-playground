apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audit-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/johan-vandeweerd-hs/cluster-playground
    path: applications/audit-service
    targetRevision: observability
    helm:
      valuesObject:
        kafka:
          address: ${kafka.address}
          topic: ${kafka.topic}
  destination:
    server: ${clusterArn}
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
